#include <stdio.h>
void add(int *a, int *b, int *sum) 
{
    *sum = *a + *b;
}
int main() 
{
    int a, b, sum;
    printf("enter two numbers: ");
    scanf("%d%d", &a, &b);
    
    add(&a, &b, &sum);
    
    printf("Sum of %d and %d is %d\n", a, b, sum);
    return 0;
}
