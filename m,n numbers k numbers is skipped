#include <stdio.h>

int main() 
{
    int m, n, k, i;

    printf("Enter m: ");
    scanf("%d", &m);
    printf("Enter n: ");
    scanf("%d", &n);
    printf("Enter k: ");
    scanf("%d", &k);

    for (i=m;i<=n;i++)
    {
        printf("%d ", i);
        i += k;
    }

    return 0;
}
